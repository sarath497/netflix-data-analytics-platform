{{ config(materialized='view') }}
WITH RAW_RATINGS AS(
    SELECT * FROM NETFLIXDATASET.RAW.RAW_RATINGS
)
SELECT USERID AS USER_ID, MOVIEID AS MOVIE_ID, RATING, to_timestamp_ltz(TIMESTAMP) AS TIMESTAMP
FROM RAW_RATINGS
